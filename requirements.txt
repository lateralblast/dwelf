# Core - always required
selenium>=4.6.0

# Default browser engine (--engine uc, undetected-chromedriver)
undetected-chromedriver
setuptools  # provides the distutils shim undetected-chromedriver needs on Python 3.12+

# --engine selenium and --engine firefox need no extra packages beyond
# selenium itself; --engine firefox additionally needs a non-snap Firefox
# + geckodriver installed separately - see README.md.

# --display table
terminaltables
